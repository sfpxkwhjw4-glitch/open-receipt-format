{
  "name": "open-receipt-format",
  "version": "0.1.0",
  "description": "A minimal, format-agnostic schema for agent action receipts — spec + zero-dependency reference implementation.",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "node --test"
  }
}
